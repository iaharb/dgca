const fs=require('fs'); const path=require('path'); function walk(dir) { fs.readdirSync(dir).forEach(file => { let f=path.join(dir, file); if(fs.statSync(f).isDirectory()) walk(f); else if(f.endsWith('.ts') || f.endsWith('.tsx')) { let text = fs.readFileSync(f, 'utf8'); if(text.includes('\\\'carriers\\\'')) { fs.writeFileSync(f, text.replaceAll('\\\'carriers\\\'', '\'carriers\'')); console.log('Fixed', f); } } }) }; walk('./src')
